import type {FC} from 'react';

import {Counter} from '@/lib';
import './index.css';

const App: FC = () => {
    return (
        <div>
            <Counter />
        </div>
    );
};

export default App;
